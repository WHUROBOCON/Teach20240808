include_directories(.)

project(stack)
add_executable(stack src/stack.c)

project(queue)
add_executable(queue src/queue.c)

project(list)
add_executable(list src/list.c)

project(doublelist)
add_executable(doublelist src/doublelist.c)